# Leave blank for local FastAPI serving the frontend and API from the same origin.
# For a Vercel frontend with a separate deployed backend, set this to the backend origin.
#
# Example:
# VITE_API_BASE_URL=https://wearable-api.example.com

VITE_API_BASE_URL=
